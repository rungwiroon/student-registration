/**
 * Route constants for E2E tests.
 * Single source of truth — change here if routes move.
 */

export const routes = {
  dashboard: '/dashboard',
  classList: '/class-list',
  contacts: '/contacts',
  register: '/register',
  editProfile: '/profile/edit',
  document: '/document',
};
